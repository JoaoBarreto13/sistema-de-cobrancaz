/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
  // Required for Replit proxy
  allowedDevOrigins: ['*'],
}

export default nextConfig
